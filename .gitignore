# private working notes — keep local, never publish
SESSION_LOG.md
CLAUDE.md
NOTES.md
WEBPV-AUDIT-PROTOCOL.md
REACTBITS.md
CLIENT-PLAYBOOK.md
DEALS.md
LAB-STYLES.md
FASTWORK-GIG.md
FASTWORK-COVER-BRIEF.md
fastwork-covers/
# the two scripts that build the covers: they carry the gig's own wording
# and only ever write into fastwork-covers/, which is private too
web/scripts/make-covers.mjs
web/scripts/find-screens.mjs
avatar-512.png

# leftover empty scaffold folder — real app is web/
webpv/

# disabled scaffold git dir (renamed aside from web/.git)
web/.git_disabled_scaffold/

# dev artifacts — screenshots from verify scripts.
# Two paths on purpose: most scripts write to web/screenshots/ when run from
# the repo root, but shoot-audit resolves its output against the cwd and
# lands one level up.
web/screenshots/
screenshots/
# shoot-unfold.mjs wrote here before 2026-09-22; kept so an old checkout stays clean
web/shots/
shots/

# local state written by `wrangler dev`
.wrangler/
FASTWORK-ABOUT-BRIEF.md
FASTWORK-FORM.md

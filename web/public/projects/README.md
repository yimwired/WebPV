# Project preview images

Drop screenshots here, then reference them in `src/lib/projects.ts` via the
`image` field, e.g.:

```ts
image: "/projects/aurum.png",
```

Recommended: 16:9, ~1280×720, PNG/JPG/WebP. If a project has no `image`, the
card shows a styled placeholder automatically.
